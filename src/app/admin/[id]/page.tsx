// Admin edit content form — to be implemented in Issue #10

export default function EditContentPage({ params }: { params: { id: string } }) {
  return (
    <main>
      <p>Edit content {params.id} — to be implemented in Issue #10</p>
    </main>
  )
}
